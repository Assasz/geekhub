{% extends 'layout/default.html.twig' %}

{% block title %}GeekHub{% endblock %}

{% block content %}
    {{ include('site/chat.html.twig') }}
    {{ render(controller('AppBundle:Post:popular')) }}

    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        {{ render(controller('AppBundle:Post:recommended')) }}
        {{ render(controller('AppBundle:Post:followed')) }}
    {% endif %}

    {{ render(controller('AppBundle:Post:latest')) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/app-index.js') }}"></script>
{% endblock %}
