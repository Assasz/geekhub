{% extends 'layout/default.html.twig' %}

{% block title %}GeekHub{% endblock %}

{% block content %}

{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
    {{ render(controller('AppBundle:Post:followed')) }}
{% endif %}

{{ render(controller('AppBundle:Post:popular', {'number': 6})) }}
{{ render(controller('AppBundle:Post:latest', {'number': 6})) }}

{% endblock %}
